version: v1beta2
deployments:
  - name: remote-dependency-10
    component:
      containers:
        - image: nginx
hooks:
- command: echo
  args:
  - ${DEVSPACE_GIT_COMMIT}
  when:
    before:
      deployments: all
