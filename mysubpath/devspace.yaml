version: v1beta3
images:
 default:
   image: dscr.io/test/devspace
   createPullSecret: true
deployments:
 - name: subDirDependency
   component:
     containers:
       - image: dscr.io/test/devspace
