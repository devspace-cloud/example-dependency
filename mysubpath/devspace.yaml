version: v1beta3
images:
 default:
   image: dscr.io/${DEVSPACE_USERNAME}/devspace
   createPullSecret: true
deployments:
 - name: subDirDependency
   component:
     containers:
       - image: dscr.io/${DEVSPACE_USERNAME}/devspace
